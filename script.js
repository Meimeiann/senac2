// Apenas para confirmação simples no envio
document.addEventListener("DOMContentLoaded", () => {
    const form = document.querySelector(".evento-form");
    form.addEventListener("submit", () => {
        alert("Solicitação enviada com sucesso!");
    });
});
